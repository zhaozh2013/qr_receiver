# QR Receiver PWA (GitHub Pages Ready)

Deploy these files to the root of a GitHub repository and enable GitHub Pages from `main` branch, `/(root)`.

Then open:

`https://<your-username>.github.io/<repo-name>/`

in Android Chrome and install the app.
